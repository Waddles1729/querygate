node_modules/
dist/
demo/*.db
demo/*.jsonl
*.log
.DS_Store
coverage/

# Not committed: CI installs against the published ranges, so an upstream
# break shows up here rather than in someone else's first `npm install`.
package-lock.json
